# データを検索できる

## 2.全カラムの取得

- SELECT \* FROM <テーブル名>

## 3.カラムの選択

- SELECT <カラム名> FROM <テーブル名>

## 4.カラム名の別名

- SELECT <元のカラム名> AS <別名> FROM <テーブル名>

## 5.重複行の削除

- SELECT DISTINCT <重複を排除したいカラム名> FROM <テーブル名>
